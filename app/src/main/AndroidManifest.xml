<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.markjlehman.todolist" >

    <application
        android:name="com.activeandroid.app.Application"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <activity android:name=".ui.CategoryActivity" >
                <meta-data
                    android:name="android.support.ui.MainActivity"
                    android:value="com.markjlehman.todolist.ui.MainActivity" />
            </activity>
        </activity>

        <meta-data
            android:name="AA_DB_NAME"
            android:value="Todo.db" />
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="3" />

        <activity
            android:name=".ui.CategoryActivity"
            android:label="@string/title_activity_category" >
        </activity>
        <activity
            android:name=".ui.FinishedTasksActivity"
            android:label="@string/title_activity_finished_tasks"
            android:parentActivityName=".ui.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.markjlehman.todolist.ui.MainActivity" />
        </activity>
    </application>

</manifest>
